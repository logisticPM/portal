node_modules
.next
out
.env.local
*.log
.DS_Store
next-env.d.ts
*.tsbuildinfo

# SST (Ion) generated local state + types — regenerated by `sst dev` / `sst deploy`
.sst
sst-env.d.ts
# OpenNext build artifacts (produced by `sst deploy`)
.open-next

# third-party reference PDFs — kept locally, not committed (bloat + copyright)
docs/specs/*.pdf
